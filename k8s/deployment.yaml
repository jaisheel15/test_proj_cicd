apiVersion: apps/v1
kind: Deployment
metadata:
  name: session17-python
spec:
  replicas: 2
  selector:
    matchLabels:
      app: session17-python
  template:
    metadata:
      labels:
        app: session17-python
    spec:
      containers:
        - name: session17-python
          image: jaisheel15/test_proj_cicd:latest
          imagePullPolicy: Always           # pull from Docker Hub
          ports:
            - containerPort: 5001
